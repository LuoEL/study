1.线性回归

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

path = 'ex1data1.txt'   #提取路径
data = pd.read_csv(path,header=None,names=['populartion','profit']) #如果不写header，默认是第一行位表头名称
data.head() #显示前面五行

data.describe() #显示样本数、平均值、标准差、最小值、分位数、最大值

data.plot(kind='scatter',x='populartion',y='profit',figsize=(12,8)) #绘制散点图，默认是kind='line'折线图；x和y的名字必须和上面一样；最后是宽度和长度
plt.show()  #显示

#代价函数
def computeCost(X, y, theta):
    inner = np.power(((X * theta.T) - y), 2)
    return np.sum(inner) / (2 * len(X))

data.insert(0, 'Ones', 1)   #在第0列插入，0列名字改为Ones,数据全是1

cols = data.shape[1]    #0是行数，1是列数
X = data.iloc[:,0:cols-1]   #逗号前面是取行，后面是取列
y = data.iloc[:,cols-1:cols]

X = np.matrix(X.values) #创建数组
y = np.matrix(y.values)
theta = np.matrix(np.array([0,0]))  #创建一个1*2的0数组

#计算梯度下降
def gradientDescent(X, y, theta, alpha, iters):
    temp = np.matrix(np.zeros(theta.shape))
    par = int(theta.ravel().shape[1])
    cost = np.zeros(iters)

    for i in range(iters):
        error = X*theta.T - y
        for j in range(par):
            term = np.multiply(error,X[:,j])
            temp[0,j] = theta[0,j] - ((alpha/len(X))*np.sum(term))

        theta = temp
        cost[i] = computeCost(X, y, theta)

    return theta,cost

alpha = 0.01    #设置学习速率
iters = 1000    #设置次数

x = np.linspace(data.populartion.min(), data.populartion.max(), 100)    #设置x轴
f = g[0,0]+g[0,1]*x                                                     #设置y轴
fig,ax = plt.subplots(figsize=(12,8))                                   #设置图
ax.plot(x, f, 'r', label='prediction')                                  #划线
ax.scatter(data.populartion, data.profit, label='data')                 #画散点
ax.legend(loc=2)                                                        #设置标签位置（1234；右上；左上；左下；右下）
ax.set_xlabel('populartion')                                            #设置标签
ax.set_ylabel('profit')
ax.set_title('VS')
plt.show()                                                              #展示图

fig,ax = plt.subplots(figsize=(12,8))
ax.plot(np.arange(iters), cost, 'r')
plt.show()