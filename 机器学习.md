0.python知识点
import matplotlib.pyplot as plt #导入绘图库
fig, ax = plt.subplots(figsize=(12, 8)) #创建画布和子图

ax.plot(x,y,c='r',ls='-',lw=1.5,marker='o',markersize=5,label='111',alpha=0.7)
#x=x轴数据,y=y轴数据,c=颜色,ls=线形状,lw=线宽,marker=数据点标记模样,markersize=标记大小,label=图例标签,alpha=透明度

sc = ax.scatter(size, bedroom, price_true, c='blue', marker='o', alpha=0.6, label='真实数据')   #绘制散点图
#x坐标数据必填，y坐标数据必填，z坐标数据（3D），s 点的大小20，c 颜色自动，marker点的形状'o'，alpha透明度（0-1）1.0，label图例标签None，cmap颜色映射None


import pandas as pd #导入文件读写库
DataFrame 是 pandas 库中的 二维表格型数据结构
┌─────────────────────────────────┐
│  列名:  name    age   score     │  ← 表头（列索引）
├─────────────────────────────────┤
│  行0:   张三    25     85       │
│  行1:   李四    30     92       │  ← 数据
│  行2:   王五    22     78       │
└─────────────────────────────────┘
        ↑ 行索引（自动生成的0, 1, 2...）
data = pd.read_csv(...)   # 得到一个 DataFrame
data['exam1']             # 得到一个 Series
data[['exam1', 'exam2']] → 两列，仍是 DataFrame，形状 (100,2)
data['exam1'] → 单列，Series，形状 (100,)
data.iloc[:, 0:3] → 取前三列，DataFrame
data.iloc[:, 3] → 单列，Series


import numpy as np  #导入数组库
arr = np.array([1, 2, 3])        # 创建数组
arr2 = np.zeros((3, 4))           # 创建3x4全零矩阵
arr3 = np.ones((2, 3))            # 创建2x3全1矩阵
x.values    #获取DataFrame底层的numpy数组   <class 'numpy.ndarray'>
np.matrix() #将数组转换为矩阵   <class 'numpy.matrix'>
theta = np.matrix(np.array([0, 0, 0]))  #创建numpy数组
np.power(x,n)   #x底数数组  n指数   #等价x ** 2 
np.sum(a,axis=1)  #a数组  axis不填全部，0=按列，1=按行
np.multiply(x1,x2)  #相乘
np.arange(start，stop，step) #生成等差数组，开始默认0，结束必填，步长默认1
np.linspace(start，stop，num)   #生成等间隔的数值序列   起始必填，结束必填，生成点数默认50
size = x[:, 1].A1   #将矩阵转换为一维numpy数组（展平）


df.mean(axis=1) #参数axis 计算方向，0=按列，1=按行 
data.insert(0, 'ones', 1)   #指定位置插入新列
data.shape[1]   #返回DataFrame的形状（行数，列数）  0是行，1是列
df.iloc[行开始:行结束, 列开始:列结束]       #x = data.iloc[0:2, 0:3]
len()   #返回对象的长度或元素个数，多维数组返回行数
range(x,y,z) #生成整数序列，x起始默认0，y结束必填，z步长

np.exp(n)   #e的n次方

import scipy.optimize as opt
opt.fmin_tnc(func, x0, fprime, args)
#func 最小化的目标函数  x0初始参数向量  fprime计算梯度的函数    args传递给 func 和 fprime 的额外参数元组,例如 args=(x, y)


1.线性回归

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

path = 'ex1data1.txt'   #提取路径
data = pd.read_csv(path,header=None,names=['populartion','profit']) #如果不写header，默认是第一行位表头名称
data.head() #显示前面五行

data.describe() #显示样本数、平均值、标准差、最小值、分位数、最大值

data.plot(kind='scatter',x='populartion',y='profit',figsize=(12,8)) #绘制散点图，默认是kind='line'折线图；x和y的名字必须和上面一样；最后是宽度和长度
plt.show()  #显示

#代价函数
def computeCost(X, y, theta):
    inner = np.power(((X * theta.T) - y), 2)
    return np.sum(inner) / (2 * len(X))

data.insert(0, 'Ones', 1)   #在第0列插入，0列名字改为Ones,数据全是1

cols = data.shape[1]    #0是行数，1是列数
X = data.iloc[:,0:cols-1]   #逗号前面是取行，后面是取列
y = data.iloc[:,cols-1:cols]

X = np.matrix(X.values) #创建数组
y = np.matrix(y.values)
theta = np.matrix(np.array([0,0]))  #创建一个1*2的0数组

#计算梯度下降
def gradientDescent(X, y, theta, alpha, iters):
    temp = np.matrix(np.zeros(theta.shape))
    par = int(theta.ravel().shape[1])
    cost = np.zeros(iters)

    for i in range(iters):
        error = X*theta.T - y
        for j in range(par):
            term = np.multiply(error,X[:,j])
            temp[0,j] = theta[0,j] - ((alpha/len(X))*np.sum(term))

        theta = temp
        cost[i] = computeCost(X, y, theta)

    return theta,cost

alpha = 0.01    #设置学习速率
iters = 1000    #设置次数

x = np.linspace(data.populartion.min(), data.populartion.max(), 100)    #设置x轴
f = g[0,0]+g[0,1]*x                                                     #设置y轴
fig,ax = plt.subplots(figsize=(12,8))                                   #设置图
ax.plot(x, f, 'r', label='prediction')                                  #划线
ax.scatter(data.populartion, data.profit, label='data')                 #画散点
ax.legend(loc=2)                                                        #设置标签位置（1234；右上；左上；左下；右下）
ax.set_xlabel('populartion')                                            #设置标签
ax.set_ylabel('profit')
ax.set_title('VS')
plt.show()                                                              #展示图

fig,ax = plt.subplots(figsize=(12,8))
ax.plot(np.arange(iters), cost, 'r')
plt.show()



2.逻辑回归
无正则化
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import scipy.optimize as opt

path = 'ex2data1.txt'
data = pd.read_csv(path,header=None,names=['exam1','exam2','admitted'])

positive = data[data['admitted'].isin([1])]
negative = data[data['admitted'].isin([0])]
fig,ax = plt.subplots(figsize=(12,8))
ax.scatter(positive['exam1'], positive['exam2'], c='r', marker='o', label='admitted')
ax.scatter(negative['exam1'], negative['exam2'], c='b', marker='x', label='not admitted')
ax.legend(loc=1)
ax.set_xlabel('exam1')
ax.set_ylabel('exam2')

def sigmoid(z):
    return 1/(1+np.exp(-z))

num = np.arange(-10, 10, step=1)
fig,ax = plt.subplots(figsize=(12,8))
ax.plot(num,sigmoid(num),c='r')
plt.show()

def cost_logic(theta, x, y):
    h = sigmoid(x@theta)
    first = -y*np.log(h)
    second = -(1-y)*np.log(1-h)
    return np.sum(first+second)/len(x)

data.insert(0, 'ones',1)
x = data.iloc[:,0:3]
y = data.iloc[:,3:4]
x = np.array(x.values)
y = np.array(y.values).ravel()   
theta = np.zeros(3)

def descent(theta, x, y):
    par = len(theta)
    grad = np.zeros(par)
    m = len(y)

    error = sigmoid(x@theta) - y
    for i in range(par):
        temp = np.multiply(error, x[:, i])
        grad[i] = np.sum(temp)/m
    
    return grad

result = opt.fmin_tnc(func=cost_logic, x0=theta, fprime=descent, args=(x,y))
theta = result[0]

def predict(theta, x):
    probability = sigmoid(x@theta)
    return[1 if i>=0.5 else 0 for i in probability]
    
prodictions = predict(theta, x)
correct = [1 if ((a==1 and b==1)or(a==0 and b==0)) else 0 for (a,b) in zip(prodictions,y)]
accuracy = np.sum(correct)/len(correct)
print('accuracy = {:.2%}'.format(accuracy))

正则化
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import scipy.optimize as opt

path = 'ex2data2.txt'
data = pd.read_csv(path, header=None, names=('test1', 'test2', 'passed'))

passtest = data[data['passed'].isin([1])]#不熟
nopasstest = data[data['passed'].isin([0])]

fig,ax = plt.subplots(figsize=(12,8))
ax.scatter(passtest['test1'], passtest['test2'], c='r', marker='o', label='passed')#不熟
ax.scatter(nopasstest['test1'], nopasstest['test2'], c='b', marker='x', label='nopassed')
ax.legend()
ax.set_xlabel('test1')
ax.set_ylabel('test2')

data.insert(3, 'ones',1)
max_degree = 6
count = max_degree + 1

x1 = data['test1']
x2 = data['test2']

for i in range(1,count):
    for j in range(0,i+1):
        data['F'+str(i-j)+str(j)] = np.power(x1, i-j) * np.power(x2, j)

data.drop('test1', axis=1, inplace=True)
data.drop('test2', axis=1, inplace=True)
data.head()

def sigmoid(z):
    return 1/(1+np.exp(-z))

def cost(theta, x, y, learningRate):
    m = len(y)
    h = sigmoid(x@theta)
    first = -y*np.log(h)
    second = -(1-y)*np.log(1-h)
    three = theta[1:]**2
    return np.sum(first+second)/m + (learningRate*np.sum(three))/(2*m)

def gradientReg(theta, x, y, learningRate):
    m = len(y)
    grad = np.array(theta).ravel()
    error = sigmoid(x@theta)-y
    for i in range(len(theta)):
        temp = error*x[:,i]
        if (i==0):
            grad[i] = np.sum(temp)/m
        else:
            grad[i] = (learningRate/m)*theta[i] + np.sum(temp)/m
    return grad

clos = data.shape[1]
x = data.iloc[:,1:clos]
y = data.iloc[:,0:1]

x = np.array(x.values)
y = np.array(y.values).ravel()
theta = np.zeros(x.shape[1])

learningRate = 1

result = opt.fmin_tnc(func=cost, x0=theta, fprime=gradientReg, args=(x, y, learningRate))

def predict(theta, x):
    probability = sigmoid(x@theta)
    return[1 if i>=0.5 else 0 for i in probability]

theta_min = np.array(result[0])
predictions = predict(theta_min, x)
correct = [1 if((a==1 and b==1)or(a==0 and b==0)) else 0 for (a, b) in zip(predictions, y)]
accuracy = np.sum(correct)/len(correct)
print('accuracy = {:.2%}'.format(accuracy))

# ========== 绘图决策边界 ==========
def map_feature(x1_vals, x2_vals, degree):
    m = len(x1_vals)
    out = np.ones((m, 1))
    for i in range(1, degree+1):
        for j in range(0, i+1):
            col = np.power(x1_vals, i-j) * np.power(x2_vals, j)
            out = np.hstack((out, col.reshape(-1, 1)))
    return out

# 创建网格
u = np.linspace(x1.min(), x1.max(), 100)
v = np.linspace(x2.min(), x2.max(), 100)
xx, yy = np.meshgrid(u, v)

# 生成特征矩阵（使用相同的 max_degree）
grid_points = map_feature(xx.ravel(), yy.ravel(), max_degree)
z = sigmoid(grid_points @ theta_min)   # theta_min 必须是一维
z = z.reshape(xx.shape)

# 绘图
fig2, ax2 = plt.subplots(figsize=(12,8))
ax2.scatter(passtest['test1'], passtest['test2'], c='r', marker='o', label='passed')
ax2.scatter(nopasstest['test1'], nopasstest['test2'], c='b', marker='x', label='not passed')
ax2.contour(xx, yy, z, levels=[0.5], colors='g', linewidths=2)
ax2.legend()
ax2.set_xlabel('test1')
ax2.set_ylabel('test2')
ax2.set_title(f'Decision Boundary (max_degree={max_degree})')
plt.show()